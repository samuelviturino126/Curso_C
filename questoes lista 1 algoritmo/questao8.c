#include <stdio.h>0

int main(void){int A, B;
printf("Digite o valor de A e B");
scanf("%d%d", &A, &B);
if(A>B){
    printf("A e maior que B");}else {
        if(A==B){printf("A=B");} else{printf("A e menor que B");
    }}
    return 0;
}